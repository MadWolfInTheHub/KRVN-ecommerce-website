import React, { FC } from 'react';
import './footer.scss'
const Footer: FC = () => {
  return (
    <footer className='footer-container'>
      some links here
    </footer>
  );
};

export default Footer;